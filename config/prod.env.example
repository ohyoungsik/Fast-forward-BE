ENV=prod
DEBUG=false
PROJECT_NAME=Fast-Forward Backend

# DB_URL, SECRET_KEY 등 민감한 값은 서버의 .env.prod → .env 를 통해 os.environ으로 주입됩니다.
# 아래 값을 직접 채워도 되지만, .env.prod 에 설정하는 것을 권장합니다.
# DB_URL=postgresql://<DB_USER>:<DB_PASSWORD>@<DB_HOST>:5432/<DB_NAME>
# SECRET_KEY=<CHANGE_ME>
